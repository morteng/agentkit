# Third-Party Notices

This project depends on the following open-source packages. All are permissively licensed (MIT / Apache-2.0 / BSD).

- `anthropic` — MIT
- `openai` — Apache-2.0
- `mcp` — MIT
- `pydantic`, `pydantic-settings` — MIT
- `redis-py` — MIT
- `httpx` — BSD
- `python-ulid` — MIT
- `structlog` — Apache-2.0 / MIT

Dev-only dependencies (pytest, ruff, pyright, etc.) are not listed here. See `pyproject.toml` for the complete dev set.
